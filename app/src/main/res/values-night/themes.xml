<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Prova" parent="Theme.MaterialComponents.DayNight">
        <!-- Altre impostazioni del tema -->

        <!-- Aggiungi o modifica questa linea per cambiare il colore di sfondo dei pulsanti -->
        <item name="colorPrimaryDark">@color/violet</item>
        <item name="colorPrimary">@color/violet</item>
        <item name="colorAccent">@color/violet</item>
        <item name="android:textColorPrimary">@color/white</item>
    </style>

</resources>